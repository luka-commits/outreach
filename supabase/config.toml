project_id = "gdlkwpmhxjaxialgvdyc"

[functions.scrape-url]
# JWT verification handled manually in function code (getUser validation)
# Gateway JWT check disabled - auth is enforced in function code via getUser()
verify_jwt = false

[functions.start-scrape]
# JWT verification handled manually in function code (getUser validation)
# Keeps verify_jwt=false for compatibility but auth is still enforced
verify_jwt = false

[functions.scrape-callback]
# Callback from Modal webhook - uses secret-based auth instead of JWT
verify_jwt = false

[functions.gmail-oauth-callback]
# JWT verification handled manually in function code (getUser validation)
# Gateway JWT check disabled to avoid token timing issues with OAuth popup flow
verify_jwt = false

[functions.send-email]
# JWT verification handled manually in function code (getUser validation)
verify_jwt = false
